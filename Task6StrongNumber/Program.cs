using System;

namespace Task6StrongNumber
{
    internal class Program
    {
        /*
         * WiP
         */

        /*
        static void Main(string[] args)
        {
            int strongNumber =Int32.Parse(Console.ReadLine());
            int sum=0;
            string[] numberToStrings = {strongNumber};

            for(int i=0;i<numberToStrings.Length;i++)
            {
                int buff = Convert.ToInt32(numberToStrings[i]);
                sum = sum + buff * buff;
            }

            //Convert.ToInt32(strongNumber);


            if(sum == strongNumber)
            {
                Console.WriteLine("yes");
            }
            else
            {
                Console.WriteLine("no");
            }
        }
        */
    }
}